<h2>Listado de preguntas:</h2>
<div id="list">
    <ul id="lista">
    <% for(var i=0; i<prg.length; i++) {%>
       <li><%- link_to(prg[i], '/quizes/questions/'+(i+1)) %></li>
    <% } %>
    </ul>
</div> 